# S02: TCP Server

Create the TCP server layer using Node.js `net.createServer`. Accept client connections, pipe data through the RESP parser, dispatch parsed commands, and write serialized responses back. Handle connection lifecycle, pipelining, and backpressure.

---

[← Back to S02](README.md)
