# T01: Lua VM Integration

**Status:** done

Set up Lua VM abstraction layer (LuaEngine interface) to allow swapping VM implementations. Initialize VM with wasmoon-lua5.1 (or fengari fallback). Manage VM lifecycle.

## Acceptance Criteria

- Lua VM initializes
- Executes basic Lua code
- Clean shutdown

---

[← Back](README.md)
